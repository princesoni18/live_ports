import 'package:flutter/material.dart';

class ProjectBuilderDesktop extends StatelessWidget {
  const ProjectBuilderDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

        body: Center(
          child: Text("project"),
        ),
    );
  }
}